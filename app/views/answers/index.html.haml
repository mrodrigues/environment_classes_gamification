%h1 Listing answers

%ul
  - @answers.each do |answer|
    %li
      = answer.city.name
      = link_to t(:show), "#answer_#{answer.id}", 'data-toggle' => 'collapse', class: 'btn'
      .collapse{id: "answer_#{answer.id}"}
        %p= answer.text
        - if answer.result.present?
          = link_to t(:edit_result), edit_result_path(answer.result), class: 'btn'
        - else
          = link_to t(:give_result), new_result_path(answer_id: answer), class: 'btn'

%br

