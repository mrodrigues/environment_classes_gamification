%h2= Problem.model_name.human
%p= @problem.text
= simple_form_for(@answer) do |f|
  = f.error_notification

  .form-inputs
    = f.input :problem_id, as: :hidden
    = f.input :text, as: :text, input_html: { class: 'input-xxlarge', rows: 6 }

  .form-actions
    = f.button :submit
